let g:omnipytent_filePrefix = '.lapin' " Replace with your own (user)name
let g:omnipytent_defaultPythonVersion = 3 " Or 2, if you want to use Python 2

"prevent the teminal to automatically close when entering keys
" tnoremap <Esc> <C-\><C-n>
" au TermOpen  * setlocal nonumber | startinsert
" au TermClose * setlocal   number | call feedkeys("\<C-\>\<C-n>")
